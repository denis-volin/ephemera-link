<!DOCTYPE html>
<html lang="en" data-bs-theme="auto">
<head>
    <meta http-equiv="Content-Type" content="text/html; charset=UTF-8">
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">

    <title>Ephemera Link - Secret Created</title>

    <!-- Favicons -->
    <link rel="apple-touch-icon" sizes="180x180" href="/static/favicon/apple-touch-icon.png">
    <link rel="icon" type="image/png" sizes="32x32" href="/static/favicon/favicon-32x32.png">
    <link rel="icon" type="image/png" sizes="16x16" href="/static/favicon/favicon-16x16.png">
    <link rel="manifest" href="/static/favicon/site.webmanifest">

    <!-- Styles and fonts -->
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <link href="/static/styles.css" rel="stylesheet">
</head>
<body>
    <div class="center-container">
        <h1 class="text-center mt-4">Ephemera Link</h1>
        <div class="secret-container theme-card text-center">
            <h1 class="mb-4">Secret Created</h1>
            <p class="mb-4">It can only be viewed once and will expire in
                <br><strong id="expire">{{.expire}}</strong></p>
            <p class="mb-4">
                <a id="copy" class="link-opacity-100" href="{{.link}}">{{.link}}</a>
            </p>

            <div class="d-grid gap-2">
                <button id="copyBtn" class="btn btn-primary">Copy Link to Clipboard</button>
                <a href="/" class="btn btn-outline-secondary">Create Another Secret</a>
            </div>
        </div>
    </div>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
    <script src="/static/saved.js"></script>
    <script src="/static/clipboard.js"></script>
</body>
</html>
